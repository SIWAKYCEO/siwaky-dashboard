# Easypanel: internal Postgres (same Docker network as the backend app).
DATABASE_URL=postgres://siwaky:siwaky@siwaky_database:5432/siwaky?sslmode=disable

# If the panel shows a connection URL that differs from built-in fallbacks, set one or more here (comma-separated).
# DATABASE_URL_FALLBACKS=


# Optional: comma-separated extra CORS origins (required hosts are set in code).
# CORS_ALLOW_ORIGINS=http://localhost:3001
