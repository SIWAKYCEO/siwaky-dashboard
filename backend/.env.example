# Primary (Pydantic / FastAPI) — override in Easypanel if needed; code falls back to public Postgres IP.
DATABASE_URL=postgresql://siwaky:siwaky@187.124.3.192:5432/siwaky

# Alternatives (same value; some panels expose one of these names)
# POSTGRES_URL=
# POSTGRES_PRISMA_URL=

# Optional: comma-separated extra CORS origins (required hosts are set in code).
# CORS_ALLOW_ORIGINS=http://localhost:3001
